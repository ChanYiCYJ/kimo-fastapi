# 核心框架
fastapi>=0.115
uvicorn[standard]>=0.30

# ORM 与迁移
tortoise-orm>=0.22
aerich>=0.7
aiomysql>=0.2        # 使用 MySQL 时需安装

# 配置与校验
pydantic-settings>=2.0
email-validator>=2.0
python-multipart>=0.0.9

# 认证
PyJWT>=2.8
werkzeug>=3.0

# Markdown / 拼音（Kimo 功能）
python-markdown>=3.5
pymdown-extensions>=10.0
pypinyin>=0.50

# 测试（可选）
pytest>=8.0
httpx>=0.27
pytest-anyio>=0.0.1
